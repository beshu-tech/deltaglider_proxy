# Never commit filled secrets or rendered configs containing real values.
secrets.env
*.rendered.yaml
config.yaml

# Backup artifacts (contain LIVE prod secrets — keep local, never commit).
*.zip
secrets.json
iam.json
manifest.json
*.db
*.db.bak

# Outputs of create-maintenance-user.sh (LIVE secrets — never commit).
maintenance-user.json
prod-export.yaml
