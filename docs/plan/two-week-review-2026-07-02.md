# Two-week adversarial review — 2026-07-02

Range: `4a59b04~1..HEAD` (v1.5.0 → v1.9.3 + remediation pass, ~225 commits, +35K lines).
8 dimensions, 60 agents. 34 confirmed / 18 refuted / 16 unverified (verifier quota).
The June-29..Jul-1 subrange was previously reviewed twice and remediated; those findings excluded.

## Confirmed findings (verified against HEAD)

### 1. [BLOCKER] Per-part ranged GETs are not ETag-pinned: concurrent source overwrite yields a silently corrupted mixed-generation destination that passes verify
`src/transfer.rs:564` — dimension: streaming-replication

stream_copy_passthrough fetches each part with an independent `engine.retrieve_stream_range` (transfer.rs:564 -> storage/s3.rs:1583 get_passthrough_stream_range), which sends a plain Range GET with NO If-Match / ETag condition, and fetch_part_with_resume re-issues the same unconditional GET on retry. Scenario: replication of a 1 GiB passthrough object takes minutes over WAN with concurrency=4; a client PUTs a new same-or-larger-size version of the source key mid-copy. Parts fetched before the overwrite carry old bytes, parts after carry new bytes; each part individually passes the exact-length check (transfer.rs:585). The destination is assembled from mixed generations, then stamped with the ORIGINAL head's sha256/md5/multipart_etag (transfer.rs:443-445), and verify_destination only compares dest.file_size against handle.total_size and dest.multipart_etag against the value we ourselves stamped (transfer.rs:462-468, 594-623) — both trivially pass. Passthrough GETs never re-hash, so the frankenobject is served to clients indefinitely. Fix shape: capture the source ETag at the initial HEAD and thread it into every ranged GET as If-Match (fail the copy on 412), mirroring S3 CopySourceIfMatch.

**Verifier:** Claim verified at HEAD. The ranged part-fetch stack has no ETag/If-Match capability at any layer: Engine::retrieve_stream_range (src/deltaglider/engine/retrieve.rs:409), StorageBackend::get_passthrough_stream_range (src/storage/traits.rs:322), and the S3 impl (src/storage/s3.rs:1583-1599) which sends a plain .range() GET with no .if_match(). fetch_part_once (src/transfer.rs:552-592) discards the returned FileMetadata (_meta) without any generation comparison, and fetch_part_with_resume re-issues the same unconditional GET on retry. Destination hashes are stamped from the ORIGINAL source HEAD (

### 2. [HIGH] Non-native destinations (filesystem backend) buffer the ENTIRE object in RAM on the 'streaming' path — O(object_size), not O(concurrency x part)
`src/transfer.rs:399` — dimension: streaming-replication

The streaming gate (transfer.rs:193-195) checks only the ENCRYPTION label via destination_supports_native_multipart (engine/mod.rs:876 -> traits.rs:437, default "none"), not multipart capability. A filesystem-routed destination passes the gate, but its create_multipart_upload default returns native:false (traits.rs:365-381), so transfer.rs:399-402 retains a clone of EVERY part's Bytes and passes the full `assembled: Vec<Bytes>` to finish -> complete_multipart_upload -> put_passthrough_chunked. Scenario: replication rule S3 -> local-disk backend (the prod fixture cast includes local-disk), 20 GiB passthrough object (well under the 64 GiB max_passthrough_object_size gate at store.rs:1094) -> ~20 GiB RSS -> OOM kill. Three aggravators: (a) this path is chosen IN PREFERENCE to the bounded spooled path (stream check at transfer.rs:193 precedes spooled at :217), so the gate actively routes filesystem dests away from the memory-safe route; (b) retained clones are NOT counted by replication_part_bytes_resident (PartGuard tracks only in-flight parts), so the e2e memory gate (tests/large_object_e2e_test.rs:119, which runs against a filesystem dest) reads ~10 MiB while the process actually holds the whole object — the perf gate is blind to exactly this regression; (c) the doc comment at transfer.rs:368-369 asserts "that path isn't memory-critical (local)" which is false for large objects. Fix shape: require handle-native (or probe capability) in the gate and fall through to spooled/buffered for non-native dests.

**Verifier:** CONFIRMED at HEAD. (1) Gate: src/transfer.rs:193-194 gates streaming on should_stream_copy && destination_supports_native_multipart; the latter (engine/mod.rs:876) is backend_supports_native_multipart(label) = `label != PROXY_AES_LABEL` (transfer_plan.rs:96-98). FilesystemBackend overrides neither multipart_storage_label (default "none", traits.rs:437) nor create_multipart_upload (default native:false, traits.rs:365-381), so filesystem-routed destinations pass the gate. (2) Retention: transfer.rs ~399 keeps `Some((span.number, bytes.clone()))` for non-native, builds `assembled: Vec<Bytes>` (fu

### 3. [HIGH] Proxy-AES destinations have no working large-object path: spooled_copy applies the delta max_object_size gate to passthrough objects, and with default config the branch ALWAYS fails TooLarge
`src/deltaglider/engine/store.rs:331` — dimension: streaming-replication

transfer_plan.rs:41-44 promises proxy-AES destinations "fall back to the existing buffered/chunked path", but the actual fallback chain is: stream gate off (label aes256-gcm-proxy) -> spooled_copy fires when source_size > spool_store_threshold (transfer.rs:217) -> store_spooled_delta hard-rejects size > max_object_size (store.rs:331-336) BEFORE the delta-eligibility check, i.e. the 100 MB-default DELTA limit is applied to passthrough files whose real ceiling is max_passthrough_object_size (64 GiB, config/mod.rs:1097-1103). Because spool_store_threshold DEFAULTS to max_object_size (engine/mod.rs:801-803), every default-config activation of spooled_copy is a guaranteed TooLarge — the Phase 4.1 copy branch can never succeed unless DGP_SPOOL_THRESHOLD_BYTES is explicitly set below max_object_size. Concrete failure: streaming multipart replication legitimately creates destination objects far above max_object_size (begin_passthrough_multipart gates only on the 64 GiB ceiling, store.rs:1094); chain-replicating such a 10 GiB passthrough object onward to an at-rest-encrypted backend fails permanently every run ("Object too large", non-transient, failure ring) with no fallback — spooled_copy propagates the error instead of returning Ok(None). Fix shape: gate spooled passthrough stores on max_passthrough_object_size, and route non-delta-eligible spooled stores past the delta gate.

**Verifier:** Claim CONFIRMED at HEAD, not refuted. (1) src/deltaglider/engine/mod.rs:801-803 — spool_store_threshold() defaults to max_object_size, so with default config every spooled_copy entry (transfer.rs:217, fires only when source_size > threshold) has size > max_object_size by construction. (2) src/deltaglider/engine/store.rs:331-336 — store_spooled_delta unconditionally returns EngineError::TooLarge for size > max_object_size BEFORE the delta-eligibility check (~line 373), so the passthrough branch and its real 64 GiB ceiling (max_passthrough_object_size, config/mod.rs:1097-1103) are unreachable; d

### 4. [HIGH] Parity orphan ownership is evaluated on lite LIST metadata — on S3 destinations every rule-owned orphan is misdiagnosed as ForeignOrphan
`src/replication/parity.rs:918` — dimension: parity-verify

The dest scan sets `st.owned_by_rule = Some(event_consumer::owned_by_rule(meta, &rule.name))` on the LITE list entry. On an S3/MinIO-backed destination the lite list builds `FileMetadata::fallback` from LIST data only (storage/s3.rs:1096-1108) — `user_metadata` is always empty because S3 LIST never returns user metadata, so the provenance marker check returns false for EVERY object. Orphans are then deliberately excluded from HEAD resolution (parity.rs:943 `dst_needs_logical.retain(|dk| source.contains_key(dk))`), and `apply_logical` wouldn't update ownership anyway. Failure scenario: rule copies key K to an S3 dest, source K is later deleted, verify runs → orphan classified `Some(false)` → remediation says 'destination object was not written by this rule … delete it manually', `foreign_orphans` count inflated, and the correct guidance (RuleOwnedOrphanSourceDeleted → 'enable replicate_deletes, then re-run' / 're-run mirror-deletes it') can never surface. The Some(false)-vs-None distinction also bypasses the safer 'ownership could not be determined → ManualReview' path. Works correctly only on filesystem backends (xattr-backed lite listings carry user_metadata). Fix direction: leave ownership `None` unless real metadata was fetched, or HEAD-resolve the bounded orphan samples (≤100) before annotating.

**Verifier:** Confirmed at HEAD. src/replication/parity.rs:918 evaluates ownership on LITE list metadata (list_objects(..., false) at :805). On S3 destinations the lite path (src/storage/s3.rs bulk_list_objects:1971 / list_objects_delegated:2067 → resolve_classified_lite → fallback_metadata_from_listing → FileMetadata::fallback, src/types.rs:393) always yields user_metadata=HashMap::new() since S3 LIST never returns x-amz-meta-*; owned_by_rule (event_consumer.rs:185-190) checks user_metadata["dg-replication-rule"], so every rule-owned dest object gets Some(false). Orphans are deliberately excluded from HEAD

### 5. [HIGH] Encrypted S3 backend: parity compares ciphertext size/etag for non-delta-eligible objects — mass false ChecksumMismatch on a converged mirror
`src/replication/parity.rs:564` — dimension: parity-verify

`needs_logical_resolution` queues only `meta.is_delta() || is_delta_eligible_key(key)` for HEAD/cache resolution. `EncryptingBackend::bulk_list_objects` delegates to the inner backend unchanged (src/storage/encrypting.rs:1577), and the S3 lite list reports the STORED blob's size/etag — for an encryption-wrapped S3 backend that is the ciphertext (whole-object: +28 B IV+GCM-tag; chunked: header + ~20 B per 64 KiB frame). Failure scenario: rule replicates from a plain backend to an encrypted S3 backend (or vice versa); every non-delta-eligible object (images, media, checksum sidecars — exactly the keys the lite-list fast path trusts verbatim) compares plaintext size vs ciphertext size → `compare_pair` returns ChecksumMismatch 'size differs' (size is authoritative, parity.rs:221) → `in_sync=false` with up to 100 mismatch samples advising manual overwrite, on a mirror that is actually converged. Delta-eligible keys escape only because engine.head decrypts logical metadata. Filesystem-encrypted backends escape via xattr lite listings, so prod's encrypted-filesystem fixture hides this. `needs_logical_resolution` must also queue keys on an encryption-enabled backend (or the encrypting wrapper must overlay logical size in listings).

**Verifier:** CONFIRMED at HEAD. needs_logical_resolution (src/replication/parity.rs:564-565) queues only delta/delta-eligible keys; parity's lite list goes engine.list_objects(metadata=false) → bulk_list_objects, which for S3 builds FileMetadata from LIST data only (src/storage/s3.rs:1953-1980) and EncryptingBackend delegates it unchanged (src/storage/encrypting.rs:1577-1582) — so an aes-gcm-wrapped S3 backend reports CIPHERTEXT size/etag (whole-object +28 B IV+tag; chunked +16 B header +20 B per 64 KiB frame). compare_pair treats any size delta as authoritative ChecksumMismatch (parity.rs ~216-227), so ev

### 6. [HIGH] Box<dyn StorageBackend> blanket impl silently drops the streaming overrides for get_reference_to_file / put_reference_from_file — the whole reference/baseline is heap-loaded on every streaming-delta op
`src/storage/traits.rs:595` — dimension: engine-storage

The impl_storage_backend_for_box! macro (traits.rs:597-902) forwards every trait method EXCEPT get_reference_to_file and put_reference_from_file, which still have buffering defaults (traits.rs:134-168: get_reference→Vec→fs::write, and tokio::fs::read→put_reference). DynEngine = DeltaGliderEngine<Box<dyn StorageBackend>> (engine/mod.rs:283), so every production call — store.rs:410 (streaming baseline PUT, the M1.4 'no RAM read' fix), store.rs:433 and retrieve.rs:256 (reference materialization for streaming encode/decode) — resolves to the Box impl, falls to the trait DEFAULT, and never reaches the filesystem-hardlink / S3-stream overrides (nor RoutingBackend's forwarding at routing.rs:399/420). Failure scenario: a deployment with max_object_size raised for the streaming-delta-any-size feature does a first-in-prefix streaming PUT of a 5 GB artifact → put_reference_from_file default does tokio::fs::read of the whole 5 GB spool into RAM before put_reference (another full Vec through routing/encrypting) → container OOM-kill; likewise every streaming delta GET/PUT against that deltaspace heap-loads the 5 GB reference via the get_reference default. The 8f3bcd8 commit made the two passthrough STREAM methods required precisely to kill this bug class but left these two defaults in place on the assumption 'the 4 real backends already override both' — missing that the Box wrapper is a fifth impl every call goes through.

**Verifier:** CONFIRMED at HEAD — every factual element of the claim checks out; nothing at HEAD remediates or explicitly accepts it.

Evidence:
1. The buffering defaults exist: src/storage/traits.rs:134-143 (get_reference_to_file default = get_reference → whole Vec<u8> in RAM → tokio::fs::write) and traits.rs:159-168 (put_reference_from_file default = tokio::fs::read whole file → put_reference &[u8]).
2. The Box blanket impl drops both methods: the impl_storage_backend_for_box! macro (traits.rs:597-904, instantiated at :904) forwards get_reference, put_reference, put_reference_metadata, etc., but a grep of

### 7. [HIGH] Delta-passthrough fast path (put_delta_raw) never deletes a stale passthrough variant — deleted objects can resurrect with old content
`src/transfer.rs:911` — dimension: engine-storage

Every engine store path pairs the write with cleanup of the sibling variant (delete_passthrough_idempotent after a delta write, delete_delta_idempotent after a passthrough write — store.rs:612/725/889/1063/1210). The replication/lifecycle fast path ships the delta via engine.put_delta_raw (transfer.rs:911-919) with NO variant cleanup. Failure scenario: source key v1 stored passthrough at source (ratio lost) and replicated → dest holds `key` (passthrough); source v2 delta-compresses; next replication run takes the fast path and writes `key.delta` at dest → dest now has BOTH blobs for one logical key. resolve_object_metadata (engine/mod.rs:1141-1145) picks newest created_at — and the shipped delta's metadata is a clone of the SOURCE object's metadata (transfer.rs:846), so its created_at can lose to a later-written dest passthrough (clock skew / lifecycle-written dest), serving stale v1 content and making verify_destination fail size-check every tick (retry livelock). Even in the benign ordering, engine.delete (engine/mod.rs:1583-1599) deletes only the RESOLVED variant: DELETE removes key.delta, and the stale passthrough v1 resurrects as the live object — GET returns 200 with old bytes after a successful delete. Also permanently double-counts stored bytes in scans.

**Verifier:** CONFIRMED at HEAD. put_delta_raw (src/deltaglider/engine/mod.rs:999) is a verbatim storage.put_delta with no sibling cleanup; its sole caller is the fast path at src/transfer.rs:912, which never calls delete_passthrough_idempotent (that helper appears only in store.rs — every engine store path pairs write+sibling-cleanup at store.rs:612/725/889/977/1063/1210, the fast path uniquely does not). The gate can_delta_passthrough (transfer.rs:745) checks only the dest deltaspace REFERENCE, never the dest object variants, so a dest holding `key` (passthrough v1 from an earlier replication) gets `key.d

### 8. [HIGH] Jobs UI offers 'Run once' on paused/disabled lifecycle rules but the backend structurally 409s both
`demo/s3-browser/ui/src/jobsView.ts:127` — dimension: api-contract-ui

availableActions() pushes 'run-now' for any non-running rule, and JobsPanel.tsx:372-378 relabels it 'Run once' with title 'Run this rule once now — does not enable or resume it' whenever enabled===false || paused. That contract is true only for replication (src/api/admin/replication.rs deliberately removed the per-rule disabled/paused 409s). Lifecycle run_now still hard-409s on BOTH: src/api/admin/lifecycle.rs:117-125 ('rule is disabled (set enabled: true in YAML to run it)') and :188-199 ('rule is paused — resume it first'). Failure: operator pauses a lifecycle rule, the row shows a 'Run once' button promising a one-off, every click returns an error toast — the advertised affordance can never work. The jobsView comment and jobs-view-regression-test.mjs:68-84 enshrine the wrong cross-layer contract (they assert run-now is available when paused/disabled without distinguishing kind), so the mismatch is test-protected. Fix either side: give lifecycle run_now the same one-off semantics, or gate 'run-now' per-kind in availableActions.

**Verifier:** CONFIRMED at HEAD — the cross-layer mismatch is exactly as claimed. (1) UI side: demo/s3-browser/ui/src/jobsView.ts:122-130 `availableActions()` pushes 'run-now' for BOTH replication and lifecycle whenever `row.status !== 'running'`, with a comment asserting "the backend runs the rule once even when it's disabled or paused"; demo/s3-browser/ui/src/components/jobs/JobsPanel.tsx:371-378 relabels it 'Run once' with title 'Run this rule once now — does not enable or resume it' whenever `enabled === false || paused === true`, with no kind distinction. (2) Backend side: that one-off contract is true

### 9. [HIGH] memory_test's CompleteMultipartUpload RSS gate removed; the claimed replacement gauge does not cover that code path — invariant now unpinned
`tests/memory_test.rs:224` — dimension: test-integrity

The range replaced the gating assertion 'spike < 35 MB during CompleteMultipartUpload' with an informational eprintln, claiming the deterministic gate now lives in large_object_e2e_test::memory_bounded_resident_part_bytes. But that gate reads replication_part_bytes_resident_peak, which is bumped ONLY in src/transfer.rs (the replication streaming-copy path) — the S3-client multipart complete path (multipart.rs complete_passthrough → store_passthrough_chunked in the s3s adapter) never touches that gauge. Concrete cost: a regression that reassembles client-uploaded parts into one contiguous buffer on CompleteMultipartUpload (the exact bug the old test pinned) now merges with green CI; a 5 GB client MPU would OOM the proxy in prod with no failing test.

**Verifier:** CONFIRMED. Every factual element of the claim holds at HEAD. (1) Commit 0356dd3 (in range) removed the gating `assert!(spike < 35 * MB, ...)` from tests/memory_test.rs::test_multipart_memory_bounded and replaced it with an informational eprintln (tests/memory_test.rs:224-233), whose comment claims the deterministic gate moved to `large_object_e2e_test::memory_bounded_resident_part_bytes`. (2) That replacement test is replication-scoped: it configures a replication rule (rule_yaml, tests/large_object_e2e_test.rs:27,99) and triggers `POST /jobs/replication:e2e/run-now` (line 55), then gates on `

### 10. [HIGH] delete-vs-run liveness gate (H2) was applied to replication only — lifecycle rules can be deleted mid-run
`src/api/admin/jobs.rs:823` — dimension: design-debt

delete_rule's live-run refusal is gated on `if let (JobSubsystem::Replication, Some(db))` — for JobSubsystem::Lifecycle there is no run-status or lease check at all, and lifecycle's scheduler/run-now (src/lifecycle/scheduler.rs, src/api/admin/lifecycle.rs) also lack the post-acquire config re-check that replication got in 4f8652b. Failure: operator deletes an expire rule via DELETE /jobs/lifecycle:<name> precisely to STOP it while a scheduler-driven run is paging through a 100K-object bucket; the delete succeeds, lifecycle_reconcile_rules purges the state/lease rows out from under the live worker, and the worker keeps deleting objects for the now-deleted rule for the rest of the run, re-writing orphan cursor/run rows for a rule that no longer exists — the exact H2 shape the replication fix closed.

**Verifier:** CONFIRMED at HEAD. src/api/admin/jobs.rs:823 gates the H2 live-run refusal on `if let (JobSubsystem::Replication, Some(db))` — for JobSubsystem::Lifecycle, delete_rule performs no run-status or lease check before removing the rule and calling lifecycle_reconcile_rules (src/lifecycle/state_store.rs:85-121), which unconditionally purges lifecycle_state (including the lease row), lifecycle_run_history, and lifecycle_failures. The helpers used for replication (replication_latest_run_status, replication_lease_is_held) have no lifecycle counterparts — they don't exist. Commit 4f8652b's post-acquire 

### 11. [HIGH] Lifecycle run-now is still synchronous — the v1.9.3 'run-now hung the admin AJAX' bug class survives in the parallel subsystem
`src/api/admin/lifecycle.rs:218` — dimension: design-debt

543ca8c made replication run-now fire-and-forget (202 + background spawn) after the copyHzToBackup prod incident where the handler wedged the admin HTTP request for minutes. lifecycle::run_now still does `lifecycle::run_rule(...).await` inline and only returns Json(outcome) when the whole run finishes — a delete/transition rule over a large bucket (paged listing + per-object engine ops) blocks the jobs_action HTTP request for minutes, hitting the same axum request timeout / wedged-UI failure. The fix was applied to one of two structurally identical paths; nothing but convention says the other needs it.

**Verifier:** Claim confirmed at HEAD. src/api/admin/lifecycle.rs:218-232 awaits lifecycle::run_rule inline and returns Json(outcome) only after the full run; no tokio::spawn in the file. Replication's run_now (src/api/admin/replication.rs:176,208, from 543ca8c in range) spawns and returns 202. jobs.rs:734-758 shows the asymmetry explicitly ((Replication,RunNow) commented 'Background run — returns 202 immediately'; (Lifecycle,RunNow) awaits and returns 200). Aggravating factor: the 300s TimeoutLayer (src/startup.rs:670) cancels the future on timeout, so the lease-release at lifecycle.rs:233-236 never runs a

### 12. [HIGH] Maintenance write-gate is enforced on job runners only at run START — a live replication run keeps writing into a bucket being migrated
`src/api/admin/maintenance.rs:212` — dimension: design-debt

The gate contract has two enforcement styles: HTTP writers are blocked continuously (503) and admin bulk loops participate per-item via write_started/finished (src/api/admin/objects.rs:240 explicitly notes 'this loop can write for minutes'), but replication/lifecycle workers — the other minutes-long engine writers — check maintenance_gate.is_busy exactly once before the run and never register in-flight writes (zero write_started calls in src/replication/worker.rs, src/lifecycle/worker.rs, src/transfer.rs). Failure: a scheduler/run-now replication run into bucket X is mid-copy (e.g. the 27.9 GB prod artifact, many minutes); operator starts a migrate on X; start_migrate has no reverse check for live job runs, arms the gate, and its 'drain in-flight writes' step sees nothing to drain; replication keeps PUTting via the old route while migrate's copy pass advances past those keys → objects missing on the target after the flip, deleted with the source on cleanup. Data loss from a one-directional, start-time-only mutual exclusion held together by convention.

**Verifier:** Claim verified at HEAD. The maintenance gate's continuous enforcement is HTTP-middleware-only (startup.rs:648, gate.rs:147-173); replication/lifecycle workers and transfer.rs contain zero write_started/is_busy calls — their only gate checks are dispatch-time (replication/scheduler.rs:123, event_consumer.rs:414, api/admin/replication.rs:74, lifecycle/scheduler.rs:156, api/admin/lifecycle.rs:153) and they write via engine.store, bypassing the middleware, so drain_inflight_writes (migrate.rs:266) sees nothing from them. start_migrate (api/admin/maintenance.rs:212+) has no reverse check for live r

### 13. [HIGH] Duplicate replication rule names are still only an advisory warning, though state/cursor/lease are keyed by name — lifecycle upgraded the identical hazard to a fatal config error
`src/config_sections.rs:1682` — dimension: design-debt

lifecycle_config_errors (src/lifecycle/planner.rs:105) documents the reasoning: 'Duplicate rule names are fatal. state/cursor/lease are keyed by rule.name, so two same-named rules corrupt each other's resume cursor. Previously only an advisory warning.' replication_state is keyed by name the same way, yet validate_replication emits only a warning claiming 'the first entry wins' — which nothing enforces: the scheduler (src/replication/scheduler.rs:57) iterates ALL enabled rules with no dup-skip, so both same-named rules run back-to-back on one shared state row. Failure: two rules named 'mirror' with different sources; rule#1's continuation token is resumed against rule#2's listing, cursor and run history interleave, and arbitrary objects are silently never replicated. Same bug class, patched in one subsystem, drifting in the other.

**Verifier:** Claim verified at HEAD — the asymmetry is real and not remediated. Replication: duplicate rule names produce only an advisory warning at src/config_sections.rs:1682 ("the first entry wins"), and nothing enforces that claim: src/replication/scheduler.rs::run_due_rules (line 57) iterates ALL enabled rules with no dup-name skip, and state/cursor/lease/run-history are all keyed by rule.name (replication_ensure_state / replication_load_state / replication_try_acquire_lease at scheduler.rs:61-80). Lifecycle, by contrast, got a THREE-layer fix in this review range (commit e2f5174 "fix(lifecycle): dup

### 14. [MEDIUM] spooled_copy holds a spool reservation while store_spooled_delta awaits acquire_pair — concurrent large copies mutually starve, then fail NON-transiently
`src/transfer.rs:995` — dimension: streaming-replication

spooled_copy takes acquire(source_size) (transfer.rs:995) and holds it across store_spooled_delta, which then awaits acquire_pair(size, size) (store.rs:431) — 3x size across TWO sequential reservations. This is the hold-and-wait pattern the checklist's acquire_pair rule exists to prevent, re-created across the call boundary (the client streaming PUT at s3_adapter_s3s.rs:801-822 has the same shape). Scenario: DGP_REPLICATION_TRANSFERS=4 (default), two+ concurrent ~5 GiB delta-eligible copies against the 16 GiB default budget: each holds 5 GiB, each waits for a 10 GiB pair with <6 GiB free -> all stall 120 s (DGP_SPOOL_ACQUIRE_TIMEOUT_SECS) -> EngineError::Overloaded whose text "Service overloaded: spool budget exhausted; retry shortly" matches NOTHING in is_transient_copy_error (transfer.rs:656-683) -> the objects land as permanent failure-ring rows instead of being retried, and recur identically every run. Two independent fixes: reserve body+ref+delta as one combined reservation (or pass the held reservation down), and classify Overloaded/"retry shortly" as transient.

**Verifier:** CONFIRMED at HEAD — every load-bearing element of the claim checks out. (1) Hold-and-wait: src/transfer.rs:995 `let spool = engine.spool_acquire(source_size).await?` is held across the `store_spooled_delta` call at transfer.rs:1019-1029; store.rs:431 then awaits `self.spool_acquire_pair(size, size)` for delta-eligible keys while the caller's body reservation is still live — 3×size across two sequential reservations. The acquire_pair comment at store.rs:428-430 only covers the ref+delta pair WITHIN the function; it does not account for the caller-held body spool, so the checklist's hold-and-wai

### 15. [MEDIUM] Destination deltaspace prefix lock is held for the entire streaming copy, including all WAN source-part downloads — client PUTs to that prefix block unboundedly
`src/deltaglider/engine/store.rs:1103` — dimension: streaming-replication

begin_passthrough_multipart acquires the per-deltaspace lock and stores it in the handle (store.rs:1103, 1130), which lives through the whole buffer_unordered fetch+upload pipeline in stream_copy_passthrough (transfer.rs:336-459). Unlike the buffered path (retrieve completes BEFORE store takes the lock), the lock now spans the full source download: a 30 GiB copy at 50 MB/s holds the destination prefix's write lock ~10 minutes. acquire_prefix_lock has no timeout (engine/mod.rs:946-955), so every client PUT to any key in that destination prefix (and any other transfer into it) parks until the copy finishes or the global request timeout kills the client request with an opaque error. Operational cost: replication into a live prefix turns large-object copies into multi-minute write outages for that prefix. Fix shape: hold the lock only for begin (create) and finish (complete + metadata), or fetch parts before acquiring.

**Verifier:** Confirmed at HEAD (b6cb749): begin_passthrough_multipart acquires the per-deltaspace lock (store.rs:1103) and stores it in PassthroughMultipartHandle (_guard, ~line 1128); stream_copy_passthrough (transfer.rs:337-455) holds that handle across the entire buffer_unordered fetch+upload pipeline, so the destination prefix write lock spans all WAN source-part downloads. acquire_prefix_lock (engine/mod.rs:946-955) is a timeout-less tokio Mutex, and both client PUT paths (store.rs:140 passthrough, :172 delta) contend on the same lock — so client PUTs to the destination prefix park until the copy fini

### 16. [MEDIUM] head_burst drops any HEAD failure silently — a transient 503 during the 50-concurrency burst leaves the delta-blob size in the compare map, yielding false ChecksumMismatch
`src/replication/parity.rs:709` — dimension: parity-verify

`head_burst` maps every `engine.head` error to `None` ('missing objects (raced deletes) are simply dropped'). Two problems: (1) a transient throttle (Hetzner 503 — the exact failure 36b6c53 added LIST retry for, made MORE likely by HEAD_CONCURRENCY=50) is indistinguishable from a delete, so the key gets no logical overlay and keeps its lite ObjState — the DELTA-blob size/etag — and `compare_pair` reports 'size differs (src <delta-size> vs dst <logical-size>)' → false mismatch persisted as the durable verdict; (2) even for a genuine raced delete, the key is only dropped from RESOLUTION, not from the `source`/`dest` compare maps, so it is still classified (stale mismatch or missing) instead of being removed. The entry is not cached on failure, so the next verify self-heals — but each cold-cache scan against a throttling backend can emit dozens of false mismatches. Fix: retry transient HEAD errors like the LIST path, and remove keys whose HEAD returned NotFound from the maps.

**Verifier:** CONFIRMED at HEAD. head_burst (src/replication/parity.rs:698-718) maps every engine.head error to None with no retry and no NotFound-vs-transient distinction (line 711: Err(_) => None), while the LIST path got 5-attempt transient retry in 36b6c53 (LIST_MAX_ATTEMPTS, is_transient_copy_error) — the asymmetry is real. A key whose HEAD fails keeps its lite ObjState, which for delta objects carries the DELTA-blob size/etag (comment at lines 874-876), and compare_pair (lines 220-227) treats any size difference as authoritative ChecksumMismatch ("size differs (src X vs dst Y)"). Nothing removes HEAD-

### 17. [MEDIUM] Verify runs concurrently with a replication run of the same rule — and the UI's guided fix guarantees the race, persisting a false 'not in sync' verdict
`demo/s3-browser/ui/src/components/jobs/VerifyTab.tsx:78` — dimension: parity-verify

The parity lease (`replication_parity` table) and the run lease (`replication_state`) are fully disjoint; neither `verify` (src/api/admin/replication.rs:266) nor `run_now` checks the other. Since run-now became fire-and-forget (202 + background task), VerifyTab's fix button chains `runNow.mutate(ruleName, { onSuccess: () => start.mutate() })` — the re-verify starts the moment the run is ACCEPTED, so the source scan → dest scan walks a half-copied mirror. Failure scenario: operator sees missing_on_dest findings, clicks the advertised fix ('re-run the rule — a missing key is always copied') → verify races the copy it just started → a fresh durable 'not in sync' verdict (survives restart, shown as the rule's verification state) reporting the same objects still missing, plus false orphans from the two-phase snapshot TOCTOU (an object PUT after the source scan and event-replicated before the dest scan lists only on dest). Operator concludes the fix didn't work. Minimum fix: gate the chained start.mutate() on run settlement (replication-run-version barrier exists), or have verify report/defer when the rule's run lease is held.

**Verifier:** Confirmed at HEAD. VerifyTab.tsx:78 chains start.mutate() in runNow's onSuccess, and useRunReplicationNow (queries/jobs.ts:96) resolves on the 202 response — run_now (src/api/admin/replication.rs:49-218) acquires the replication_state lease, spawns the copy, and returns 202 immediately, so the re-verify starts while the copy runs. verify (replication.rs:266-455) gates only on parity_try_acquire_lease (replication_parity table) and never checks the run lease; run_now never checks the parity lease; parity.rs has no run-awareness. The parity result is persisted durably (parity_result_done) as the

### 18. [MEDIUM] record_fast_path_copy reads `prior` AFTER the write, so fast-path-copied objects net to ZERO in the destination bucket usage counter
`src/deltaglider/engine/mod.rs:766` — dimension: engine-storage

transfer.rs calls engine.record_fast_path_copy AFTER the with_dest_prefix_lock closure has done put_delta_raw (transfer.rs:876-937). record_fast_path_copy then calls prior_for_counter (engine/mod.rs:773), which does a live resolve_metadata of the dest key — returning the JUST-WRITTEN delta metadata, not the pre-copy state. apply_net(removed=new_meta, added=new_meta, ...) cancels exactly (bucket_usage.rs:286-318), so only seeded_ref_bytes is recorded. Failure scenario: a replication rule fast-path-ships 10,000 fresh delta objects into a dest bucket → the Ceph-style O(1) usage counter (feature 9f9077b) reports +0 objects / +0 logical / +0 stored bytes for all of them; the admin usage/savings dashboards under-report the destination indefinitely (until a full rescan reconciles). Contrast every store path, which captures prior_for_counter BEFORE the durable write (store.rs:113/338/1179). The doc comment ('Overwrite-aware... Mirrors record_store') asserts the opposite of what the code does. Fix: capture prior before (or inside, pre-write) the lock closure and thread it through.

**Verifier:** CONFIRMED at HEAD. transfer.rs:876-922 performs put_delta_raw (and optional put_reference_raw) inside with_dest_prefix_lock, then calls engine.record_fast_path_copy at transfer.rs:930-937 — after the durable write. record_fast_path_copy (src/deltaglider/engine/mod.rs:766-781) calls prior_for_counter at line 773, which does a live resolve_metadata → resolve_object_metadata (mod.rs:1102-1146, direct storage metadata reads), so for a fresh dest key it returns the JUST-WRITTEN delta metadata. apply_net (bucket_usage.rs:286-318) then computes removed(new_meta) + added(new_meta) = 0 objects / 0 logi

### 19. [MEDIUM] Admin bulk copy loop buffers each object fully in RAM and bypasses the quota gate (including quota=0 freeze)
`src/api/admin/objects.rs:293` — dimension: engine-storage

copy_one (objects.rs:293-332) does engine.retrieve → Vec<u8> → engine.store per object. Two costs: (1) memory — engine.retrieve collects even Streamed responses into a contiguous Vec (retrieve.rs:18-31), so bulk-copying a multi-GB passthrough object (legal under max_passthrough_object_size, the Phase-B streaming ceiling) buffers the entire body in RAM, exactly the OOM class transfer.rs solved with stream_copy_passthrough/spooled_copy — the admin loop predates and never adopted the bounded-memory primitive; (2) policy — the loop never calls object_helpers::check_quota, which the s3s adapter enforces on PutObject/CopyObject/CompleteMultipartUpload (s3_adapter_s3s.rs:779/890/1085). Failure scenario: an operator freezes a bucket with quota_bytes: 0 (documented hard 'freeze the bucket' override), then a GUI bulk copy/move into that bucket succeeds for all 10,000 items. Cheapest fix: route copy_one through transfer::copy_object_with_retries (gets streaming + spool for free) and add the same check_quota call the S3 plane uses.

**Verifier:** Confirmed at HEAD. (1) copy_one (src/api/admin/objects.rs:293-332) does engine.retrieve -> Vec<u8> -> engine.store per item, and retrieve (src/deltaglider/engine/retrieve.rs:11-32) collects even Streamed responses into a full-size contiguous Vec — multi-GB passthrough objects are fully RAM-buffered. (2) Zero quota references in objects.rs; check_quota (object_helpers.rs:62, incl. the quota==0 'Bucket is frozen' hard-reject) is enforced only at s3_adapter_s3s.rs:779/890/1085 and form_post.rs:891, so GUI bulk copy/move into a frozen (quota_bytes: 0) bucket bypasses the freeze. (3) Not remediated

### 20. [MEDIUM] delta_passthrough_test passes vacuously if the delta fast path never fires (violates the repo's own >0 rule)
`tests/delta_passthrough_test.rs:174` — dimension: test-integrity

Both integration tests for the new delta-passthrough replication fast path guard their fast-path assertions behind `if dp >= 1 { assert!(saved > 0) } else { eprintln!(NOTE...) }` (line 174), and the fallback test's `copied - dp >= 1` (line 249) is also satisfied when dp is always 0. The escape hatch exists for dev machines with the xdelta3 /dev/stdin quirk, but it is unconditional — including in CI where xdelta3 is present. Concrete failure: a regression that makes can_delta_passthrough always return Fallback (or makes v2 store as passthrough) removes the entire egress-savings feature; CI stays green with only a NOTE in the log. The fix pattern is already established in-repo: gate on an env (e.g. require dp>=1 when CI=true), per the 'peak<bound passes vacuously at 0 → add >0' rule from the perf-gate work.

**Verifier:** CONFIRMED at HEAD. tests/delta_passthrough_test.rs:174-188 guards the only fast-path assertion behind `if dp >= 1 { assert!(saved > 0) } else { eprintln!("NOTE: ...") }` with no CI/env gating whatsoever — the escape hatch is unconditional, including in CI where the test runs in the merge gate (`--test delta_passthrough_test`, .github/workflows/ci.yml:434, builder container with xdelta3). The fallback test's assertion at lines 248-252 (`copied - dp >= 1`) is likewise satisfied trivially when dp is always 0 and copied>=1, so BOTH tests pass green if the delta-passthrough fast path never fires an

### 21. [MEDIUM] Encryption × streaming-multipart combination has zero tests: label plumbing chain and large-object fallback to/from encrypted backends untested
`src/storage/routing.rs:657` — dimension: test-integrity

The proxy-AES gate is only unit-tested at the pure leaf (backend_supports_native_multipart against literal strings in transfer_plan.rs). The full chain — EncryptingBackend::multipart_storage_label returning 'aes256-gcm-proxy' when actively_encrypts (encrypting.rs:1379) → RoutingBackend per-bucket resolution (routing.rs:657) → Engine::destination_supports_native_multipart → the branch at transfer.rs:194 — has no test at any level, and no integration test replicates a >threshold object (a) INTO an encrypting destination (must fall back to the buffered/spooled path; a wiring bug surfaces as the create_multipart_upload defence-in-depth error, i.e. every large replication to an encrypted backend fails) or (b) FROM an encrypted source via the streaming path's concurrent ranged GETs against the DGE1 chunk math (encryption_test.rs only covers a single client-facing Range GET). A routing regression that returns the default backend's 'none' label for a routed encrypted bucket would break every large-object replication into encrypted storage with green CI.

**Verifier:** Confirmed at HEAD. The proxy-AES multipart gate is only tested at the pure leaf: transfer_plan.rs:185-191 asserts backend_supports_native_multipart against literal strings. The full chain is test-free: EncryptingBackend::multipart_storage_label (encrypting.rs:1379) and RoutingBackend::multipart_storage_label (routing.rs:657) have zero test references in src/ or tests/; Engine::destination_supports_native_multipart (engine/mod.rs:876) is called only from transfer.rs:194, and the transfer.rs tokio tests invoke stream_copy_passthrough directly, bypassing the gate in copy_object_once. Integration 

### 22. [MEDIUM] Per-object copy timeout (Phase A, ON by default at 30m) — the Elapsed branch is completely untested
`src/replication/worker.rs:761` — dimension: test-integrity

49baed5 shipped object_timeout/heartbeat/skip-after-N; skip-after-N has a state_store unit test and heartbeat is covered by the remediated lease work, but no test anywhere (grep for object_timeout in tests/ is empty) exercises the timeout Err(_elapsed) arm: it fabricates the error string 'object copy timed out after Ns', drops the copy future mid-flight (interacting with the untested Drop-abort above), and routes into the failure ledger. Notably 'timed out' matches is_transient_copy_error's needle list, so the fabricated message may be classified transient by downstream consumers — whether that produces the intended ledger/skip behavior vs a retry storm on a persistently-stalled object is unverified by any test, and this branch is live in every default prod config (default_object_timeout = "30m").

**Verifier:** Claim verified at HEAD (b6cb749). The Elapsed arm exists exactly as described at /Users/sscarduzio/me/tmp/deltaglider_proxy/src/replication/worker.rs:761-768: `tokio::time::timeout(timeout, copy_object_with_retries(...))` with `Err(_elapsed) => Err(format!("object copy timed out after {}s", ...).into())`, and default_object_timeout() = "30m" (config_sections.rs:428), so it is live in default prod configs. No test anywhere exercises this branch: grep for "object_timeout" in tests/ is empty, and no unit test in worker.rs constructs a copy with a short timeout to drive the Elapsed path. The ONLY 

### 23. [MEDIUM] Parity remediation is untested end-to-end against the foreign/newer objects the integration test itself plants
`tests/parity_test.rs:186` — dimension: test-integrity

The pure layer (analyze_finding 14-row truth table, annotate_findings, fold_actionable in src/replication/{remediation,parity}.rs) is thoroughly unit-tested, but parity_test.rs plants exactly the interesting real-world states — a foreign object ('extra.bin', line 176-193) and a dest object overwritten AFTER copy (obj-1.bin, newer-than-source) — and then asserts only counts/keys, never out['actionable'] (foreign_orphans, needs_manual) or any samples[i]['remediation'] field. The metadata→facts seam is therefore unpinned: event_consumer::owned_by_rule reading the rule marker off REAL backend metadata (parity.rs:918), and created_at extraction as millis (ObjState::from_metadata; note remediation.rs:24 still documents the field as 'unix seconds'). Timestamps in this subsystem already caused a production RCA (the created_at re-copy incident); a units or marker-plumbing regression would flip every verdict to DivergedUnknownAge/ForeignOrphan-miss and no test would notice.

**Verifier:** Claim CONFIRMED at HEAD. tests/parity_test.rs plants the foreign object ('extra.bin', put directly to parity-dst) and the tampered/newer 'obj-1.bin' (overwritten after copy), but asserts only counts and sample keys (orphan_on_dest, checksum_mismatch, *_samples[0]['key'], in_sync) — grep finds zero occurrences of 'remediation' or 'actionable' anywhere in tests/. The verify response DOES carry both (ParityOutcome at src/replication/parity.rs:1051+ includes `actionable` from fold_actionable and per-sample `remediation` from annotate_findings), so the assertions are possible but absent. The metada

### 24. [MEDIUM] run-now paused/disabled semantics diverge between replication and lifecycle, with three doc surfaces contradicting the shipped behavior
`src/api/admin/replication.rs:31` — dimension: design-debt

v1.9.3 made replication run-now a deliberate one-off that runs disabled/paused rules ('the per-rule disabled/paused 409s are intentionally gone', replication.rs:63). Contradictions at HEAD: (a) the doc comment two lines above the same function still says 'Honours the paused flag: a paused rule returns 409 Conflict'; (b) src/api/admin/lifecycle.rs:187 rejects paused with 409 and claims this is the 'same contract as replication' — false; (c) docs/product/reference/replication.md:15 says 'Disabled rules and paused rules are skipped by ... run-now alike' — the pre-1.9.3 behavior. Operator cost: the SAME Run-now button on the unified Jobs screen runs a paused replication rule but 409s a paused lifecycle rule, and every written contract an operator or the next contributor consults gives a different answer.

**Verifier:** Claim verified at HEAD on all three surfaces. (a) src/api/admin/replication.rs:30-32 doc comment still reads "Honours the paused flag: a paused rule returns 409 Conflict", directly above code whose own comments (lines 62-67 and 125-128) say the opposite: "A manual run-now is a deliberate ONE-OFF: it runs the rule once even when the rule is `enabled: false` or paused... The per-rule disabled/paused 409s are intentionally gone" and "NOTE: no paused check". The implementation confirms it: run_now only 409s on the global kill-switch, maintenance gate, or held lease — no per-rule paused/disabled ga

### 25. [MEDIUM] The run-starter sequence exists in five hand-rolled copies — background.rs never became the shared primitive, and the sprint's own history proves the cost
`src/background.rs:16` — dimension: design-debt

ensure_state → lease acquire → paused/gate ordering → post-acquire config re-check → spawn/run → release-on-every-early-exit is hand-carried in replication scheduler, replication run-now, event consumer, lifecycle scheduler, and lifecycle run-now — with different orderings (scheduler checks the maintenance gate AFTER the lease and must remember to release; run-now/event-consumer check it BEFORE). background.rs holds only RunLease + parse_duration_or. The window's own remediation log is the evidence of cost: the post-acquire re-check patch had to be applied three times (scheduler.rs:108, event_consumer.rs:438, replication.rs:131) and still missed both lifecycle sites; the lease-release-on-maintenance-defer fix likewise touched each loop separately. Findings 1, 2 and 5 above are all instances of one starter getting a fix the others didn't. Until a shared 'acquire run slot' primitive (acquire + re-check + RAII release) exists, every future invariant change is an N-site manual audit that has already failed twice.

**Verifier:** Verified at HEAD — every factual element of the claim checks out. (1) /Users/sscarduzio/me/tmp/deltaglider_proxy/src/background.rs is 51 lines and holds exactly RunLease (line 16) + parse_duration_or (line 22); no shared acquire/re-check/RAII-release primitive exists anywhere. (2) The starter sequence is hand-rolled in all five claimed sites: src/replication/scheduler.rs run_due_rules (ensure_state → paused → lease at :74 → post-acquire config re-check → gate-AFTER-lease with manual release_lease), src/api/admin/replication.rs run_now (gate-BEFORE-lease at ~:71, lease at :112, post-acquire re-

### 26. [MEDIUM] Jobs reference doc omits the kill, verify, and delete actions added later in the sprint — while asserting 405 outside the listed matrix
`docs/product/reference/jobs.md:11` — dimension: design-debt

The capability matrix lists replication: pause/resume/run-now, lifecycle: +preview, maintenance: cancel, and states 'An action outside a kind's capability matrix returns 405 with the supported list'. HEAD's JobAction (src/api/admin/jobs.rs:104-129) also parses kill, verify, and delete, plus the /jobs/:id/verify GET/POST/cancel parity routes (src/demo.rs:337-341). kill is the incident-response affordance the v1.9.x work centered on; an operator scripting from the flagship consolidated reference cannot discover it and will believe it doesn't exist. The consolidation promise ('one API surface') is only as good as the one page documenting it staying in step.

**Verifier:** Claim verified at HEAD (b6cb749). docs/product/reference/jobs.md line 11-13 capability matrix lists only pause/resume/run-now (replication), +preview (lifecycle), cancel (maintenance), and line 15 asserts "An action outside a kind's capability matrix returns 405 with the supported list"; the API table (line 25) lists only pause/resume/run-now/preview/cancel. But HEAD's code contradicts the doc: src/api/admin/jobs.rs JobAction (lines 90-113) parses verify/delete/kill, and supported_actions (lines 135-153) returns Verify+Delete+Kill for Replication and Delete for Lifecycle — so the server actual

### 27. [LOW] Arc::try_unwrap failure at multipart finish leaks the upload — guard already disarmed, no abort issued
`src/transfer.rs:449` — dimension: streaming-replication

At completion, `abort_guard.disarm()` removes the handle from the abort guard, then `Arc::try_unwrap(...)` maps a failure (another strong ref still alive) to an error and DROPS the Err(Arc) (transfer.rs:449-453). On that path neither complete nor abort ever runs: the guard is disarmed, the explicit abort branch is unreachable, and the error propagates — leaving a fully-uploaded part set dangling on backends without incomplete-upload GC (B2, the exact cost the K1 fix addressed). It also leaks the destination prefix lock guard until the stray Arc drops. Today the invariant holds (all part futures are collected before finish), but any future refactor that clones the handle past try_collect silently reintroduces the K1 dangling-upload class. Fix shape: on try_unwrap failure, call abort_passthrough_multipart_ref before returning (the by-ref abort exists precisely for this).

**Verifier:** Verified at HEAD (b6cb749): src/transfer.rs:449 disarms the abort guard then maps a try_unwrap failure to an error, dropping the Err(Arc) without calling complete or abort — the guard Drop is inert post-disarm and callers never see the handle, so a fully-uploaded part set would dangle (plus the handle's OwnedMutexGuard deltaspace lock, store.rs:28, lives until the stray Arc drops). The K1 fix (abbf51a) added by-ref abort for the cancellation and part-failure paths but not this one. The path is currently unreachable (all part-future Arc clones die at try_collect; local clone dropped at line 448

### 28. [LOW] verify_destination's multipart-ETag check is tautological on the streamed and delta-passthrough paths
`src/transfer.rs:613` — dimension: streaming-replication

For the streamed path, finish_passthrough_multipart stamps source_head.multipart_etag into the destination metadata (store.rs:1189) and the size in result.metadata is handle.total_size; verify_destination then compares the destination HEAD against those same two values (transfer.rs:606-621) — it verifies that the metadata write round-trips, not that the assembled bytes match the source (see the blocker finding for the consequence). Additionally, the spooled_copy path (transfer.rs:972-1042) is the ONLY copy strategy that performs no verify_destination at all, and mislabels passthrough sources as CopyStrategy::Reconstructed (transfer.rs:1037), skewing the per-strategy run stats operators use to confirm the fast paths are working. Cheap improvement: verify by size from a fresh un-stamped source of truth (e.g. backend content-length of the completed upload) and add the missing verify to spooled_copy.

**Verifier:** Confirmed at HEAD, not refuted. (1) Streamed path is tautological: transfer.rs:445 takes multipart_etag = source_head.multipart_etag.clone(), passes it to finish_passthrough_multipart (transfer.rs:455), which stamps it into destination metadata (store.rs:1191 `metadata.multipart_etag = multipart_etag`) and builds file_size from handle.total_size (store.rs:1183-1189, StoreResult::new(metadata, handle.total_size) @1225). verify_destination (transfer.rs:462-468) then HEADs the destination and compares against source_head.multipart_etag and result.metadata.file_size — engine.head (engine/mod.rs:12

### 29. [LOW] plan_parts empty plan + DGP_STREAM_COPY_THRESHOLD=0 sends a zero-part CompleteMultipartUpload for 0-byte objects
`src/transfer_plan.rs:112` — dimension: streaming-replication

plan_parts(0) returns an empty Vec, documented as safe because "a zero-byte object never reaches the streaming path — it's below the threshold" (transfer_plan.rs:109-111). But stream_copy_threshold() has no floor (transfer_plan.rs:67-69): an operator setting DGP_STREAM_COPY_THRESHOLD=0 (plausible when trying to 'stream everything') makes should_stream_copy(0 >= 0) true, so a 0-byte passthrough object creates a multipart upload, uploads zero parts, and calls CompleteMultipartUpload with an empty part list — S3/MinIO reject it (InvalidRequest: must specify at least one part), the copy fails non-transiently, and the object lands in the failure ring on every run. One-line fix: floor the env-resolved threshold at 1 (or return early in stream_copy_passthrough when spans.is_empty() by falling back to the buffered path).

**Verifier:** Not refuted — the claim holds at HEAD. Evidence: (1) src/transfer_plan.rs:67-69 `stream_copy_threshold()` is `env_parse_with_default("DGP_STREAM_COPY_THRESHOLD", STREAM_COPY_THRESHOLD)` with NO floor (unlike its siblings `multipart_part_size().max(S3_MIN_PART_SIZE)`, `upload_concurrency().max(1)`, `transfers().max(1)` right next to it), so `=0` parses fine and `should_stream_copy(0, "passthrough", 0)` at transfer_plan.rs:89-91 is true (0 >= 0). (2) src/transfer.rs:192-196 then routes a 0-byte passthrough object into `stream_copy_passthrough` when the destination is native-multipart; at transfe

### 30. [LOW] delete_rule ignores the parity lease: an in-flight verify survives rule deletion, keeps scanning up to 30 min, then re-inserts ghost parity rows after the purge
`src/api/admin/jobs.rs:803` — dimension: parity-verify

The H2 delete guard checks only `replication_latest_run_status` + `replication_lease_is_held` — never `parity_try_acquire_lease`'s lease on `replication_parity` — and neither sets the in-process parity cancel flag nor writes 'cancelling'. Failure scenario: operator deletes a rule while its background verify is scanning → `replication_reconcile_rules` purges the parity result row + `replication_parity_objects` cache, but the detached audit task keeps listing/HEADing the old buckets (waste, up to PARITY_LEASE_TTL_SECS=1800), then `parity_result_done`/`parity_cache_put_many` INSERT the rows right back. If the operator recreates a rule with the same name (delete-and-recreate to change source/dest is the natural workflow), the ghost verdict — computed against the OLD rule's buckets — is served as the new rule's last verification result by `verify_status`, and the resurrected cache rows are keyed only by (rule, side, dest_key) with no bucket scoping. Fix: include the parity lease in `delete_blocked_by_live_run` or flip the parity cancel signal inside the delete critical section.

**Verifier:** CONFIRMED at HEAD — every load-bearing assertion checks out in source. (1) The H2 delete guard (src/api/admin/jobs.rs:823-837, predicate delete_blocked_by_live_run at :912) consults only replication_latest_run_status + replication_lease_is_held, which reads the `replication_state` table (state_store.rs:386-398); the parity lease lives in the separate `replication_parity` table (parity_try_acquire_lease, state_store.rs:1062-1083) and is never checked. (2) delete_rule neither sets state.parity_cancels nor calls parity_request_cancel — the audit's only stop signals are the in-process flag and a '

### 31. [LOW] Multipart quota gate: only the quota=0 freeze branch tested; soft-enforce and the silent part-size undercount are unpinned
`src/s3_adapter_s3s.rs:1077` — dimension: test-integrity

quota_test.rs's new test_quota_zero_blocks_complete_multipart_upload covers only the quota=0 short-circuit in check_quota (which fires before usage data matters). The soft-enforce branch — cached usage + total_parts_size > quota on CompleteMultipartUpload — has no test, and total_parts_size is computed via `.filter_map(|(num,_)| get_part_size(upload_id, num))` which silently drops any part whose size isn't found, undercounting the incoming bytes toward the quota with no test pinning that behavior. Additionally the MPU test early-returns (eprintln, not assert) if Create/UploadPart fail, so a future strictness change degrades it to a no-op silently. A regression here is a quota bypass via multipart, invisible to CI.

**Verifier:** Claim CONFIRMED at HEAD, not refuted. (1) src/s3_adapter_s3s.rs:1077-1090: complete_multipart_upload computes total_parts_size via `.filter_map(|(num, _)| self.state.multipart.get_part_size(&input.upload_id, *num))` — get_part_size (src/multipart.rs:461) returns Option and filter_map silently drops None, so a part whose size isn't found contributes 0 bytes to the quota check (e.g. a part racing in via concurrent UploadPart between the quota gate and complete() validation). No test or unit test pins this: total_parts_size is inline in the async trait method (not a pure fn), and the only greps f

### 32. [LOW] DGP_TEST_* fault/delay seams are live in release builds with no registry entry or startup warning
`src/transfer.rs:628` — dimension: test-integrity

Four env seams ship in prod code paths: DGP_TEST_FAIL_PART_ONCE (injects a transient part failure, transfer.rs:628), DGP_TEST_PART_BARRIER/_DELAY_MS (per-part sleep after gauge bump, transfer.rs:649), DGP_TEST_OBJECT_BARRIER/_DELAY_MS (per-object sleep, replication/worker.rs:78), DGP_TEST_MAX_JOB_PAGES (clamps every pager's page budget, job_loop.rs:75). All are inert when unset, but none is in ENV_VAR_REGISTRY, none is startup-logged, and nothing warns when set — an operator copying a CI env file (the documented CI-parity workflow already tells users to export DGP_REPLAY_WINDOW_SECS=0 locally) gets silently throttled replication (150-250ms per part/object), an injected 'connection reset' per process, or truncated 'stopped' runs from a 2-page budget, with no diagnostic trail. Cheap fix: log a loud warning at boot when any DGP_TEST_* var is present.

**Verifier:** Claim confirmed at HEAD. All four DGP_TEST_* seams compile into release builds with no cfg(test)/feature gating: maybe_inject_part_failure (src/transfer.rs:628, DGP_TEST_FAIL_PART_ONCE → one injected 'connection reset (injected)' per process), maybe_part_barrier (src/transfer.rs:649, DGP_TEST_PART_BARRIER/_DELAY_MS, 150ms default per part), maybe_object_barrier (src/replication/worker.rs:77, DGP_TEST_OBJECT_BARRIER/_DELAY_MS), with_test_max_pages_env (src/job_loop.rs:74, DGP_TEST_MAX_JOB_PAGES clamps every pager built with it). None of the six var names appears in ENV_VAR_REGISTRY (src/config/

### 33. [LOW] ~10 new operator tuning env knobs shipped in the window have zero product-docs coverage; env-var docs parity is untested
`docs/product/reference/configuration.md` — dimension: design-debt

New at HEAD vs range start and absent from all of docs/product/: DGP_REPLICATION_TRANSFERS, DGP_UPLOAD_CONCURRENCY, DGP_MULTIPART_PART_SIZE, DGP_STREAM_COPY_THRESHOLD, DGP_S3_CONNECT/READ/OPERATION_ATTEMPT_TIMEOUT_SECS, DGP_S3_STALL_GRACE_SECS, DGP_MAX_PASSTHROUGH_OBJECT_SIZE (spool/codec vars appear once, in the changelog page only). These are precisely the knobs added for the large-transfer prod incidents; ENV_VAR_REGISTRY has a code-parity unit test (src/config/mod.rs:2682) but no docs-parity check, so the operator-facing reference silently decays. Cost: next slow-B2 or stalled-transfer incident, the person tuning the deployment greps the docs and concludes the knob doesn't exist.

**Verifier:** Confirmed at HEAD. All nine claimed env knobs (DGP_REPLICATION_TRANSFERS, DGP_UPLOAD_CONCURRENCY, DGP_MULTIPART_PART_SIZE, DGP_STREAM_COPY_THRESHOLD in src/transfer_plan.rs:66-84; DGP_S3_CONNECT/READ/OPERATION_ATTEMPT_TIMEOUT_SECS + DGP_S3_STALL_GRACE_SECS in src/storage/s3.rs:266-272; DGP_MAX_PASSTHROUGH_OBJECT_SIZE in src/config/mod.rs:1446) exist at HEAD, are registered in ENV_VAR_REGISTRY, and git-grep at range start 4a59b04~1 shows zero occurrences in src/ — all new in the window. Grep across docs/product/ finds none of them, while docs/product/reference/configuration.md carries 124 DGP_ 

### 34. [LOW] scripts/bench_s3_adapters.py benchmarks a retired toggle — DGP_S3_ADAPTER is inert since the axum S3 path was deleted
`scripts/bench_s3_adapters.py:111` — dimension: design-debt

The script's premise is 'Compares the legacy Axum S3 adapter with the s3s adapter' via DGP_S3_ADAPTER, but the env var is read nowhere at HEAD (only two comments reference it); both benchmark legs now exercise the identical s3s code path. Anyone running it gets a plausible-looking A/B report that measures noise, and the s3s consolidation looks incomplete to a reader who finds the toggle in scripts. Delete the script or repoint it at a real axis.

**Verifier:** Confirmed at HEAD. scripts/bench_s3_adapters.py:111 sets DGP_S3_ADAPTER per leg and runs an "axum" vs "s3s" A/B (lines 292-293), but no Rust code reads that env var — the only repo hits are two comments, and src/startup.rs:299-304 explicitly states the DGP_S3_ADAPTER selection was retired and s3s is the only S3 implementation. Both benchmark legs therefore exercise the identical s3s path and the comparison output is noise; the docstring ("Compares the legacy Axum S3 adapter with the s3s adapter") and the build hint (`--features s3s-adapter`, line 288) are stale too. Not remediated or accepted 

## Plausible but UNVERIFIED (verifier quota exhausted — re-verify before acting)

- ⌘S dead on the System page (saveModel mismatch?)
- Jobs UI action buttons offered that the API structurally 409s (partially overlaps confirmed #8)
- Path-remap regression script hand-copied table drift
- Jobs list stops polling entirely when idle(?)
- In-flight replication/lifecycle runs and engine rebuild interaction
- Spool budget hold-and-wait across call boundaries (overlaps confirmed #14)
- Replication/lifecycle live progress unreported (percent always 0 — overlaps #8 family)
- Replication and lifecycle runs pin a stale engine Arc across config applies
- Every engine-level writer bypasses the per-bucket write gate(?) (overlaps confirmed #12)
- Replication run-now success toast wording
- Migrate source-cleanup silently stops early(?)
- retain-newest collect loop ignores truncation(?)
- S3 write-path latency coupled to backend timeouts(?)
- multipart_storage_label mis-reports something(?)
- Lifecycle run-now synchronous over HTTP (overlaps confirmed #11)
- JobsPanel ignores the section editors' lock(?)
